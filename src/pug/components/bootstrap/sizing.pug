extends ../../layouts/LayoutComponent
include ../../mixins/components/bootstrap/sizing

append variables
  - var pageTitle = 'Sizing';
  - var activePage = 'sizing';

block component 

  +BootstrapComponentLink({
    link: `https://getbootstrap.com/docs/${bootstrapVersion}/utilities/sizing/`,
    name: pageTitle,
  })
  +SizingDemo
